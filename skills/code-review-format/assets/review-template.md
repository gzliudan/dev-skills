# 代码评审报告

- 版本：`<评审ID>-v<序号>（如 20260907-193012-v1）>`
- 时间：`<YYYY-MM-DD HH:MM>`
- 结论：`<通过 / 否决（+计数）>`
- 范围：
  - `<YYYY-MM-DD HH:MM:SS <hash> <title>；多 commit 父在前逐行追加>`

## 问题清单

<按 致命问题→严重问题→一般问题→轻微问题→改进意见 顺序，仅输出有条目的组；空组整体跳过，不输出组标题>

### 致命问题

#### 问题<评审ID>-<问题ID>：`<标题>`

> 例：`#### 问题20260907-190102-B1：<标题>`；同报告内可省略 评审ID 前缀短写为 `#### 问题B1：<标题>`；E 级用「意见」前缀

- 描述：`<问题>`。当 `<触发条件>` 时，`<后果>`（见 `file.go:120`）
- 代码位置：
  - `file.go:120-135`（`funcName`；历史代码加 `（存量）`）
- 引入提交：`<hash>；只写 hash；多处引入改用缩进二级列表；范围外提交加 （存量）`
- 修复建议：`<可直接执行的动作>`

  ```diff
  - <改动前>
  + <改动后>
  ```

- 验证方法：`<复现步骤 / 回归测试 / 断言点；E 级填 不适用>`
- 状态：`<新问题 / 已修复 / 未修复 / 撤回>`

## 待确认

<无待确认项 → 连同本标题整节删除，不留空节、不写「无」>

- `<事项>`：缺 `<信息>`；需确认 `<问题>`；若最坏情况成立则归 `<级别>`

## 总体结论

<由评审者自由填写>
