# 代码评审报告

- 版本号：`<v1 / v2 …>`
- 评审时间：`<YYYY-MM-DD HH:MM>`
- 评审范围：
  - `<YYYY-MM-DD HH:MM:SS <hash> <title>；多 commit 父在前逐行追加>`

## 总体结论

`<通过 / 修改后通过 / 需重大修改 / 不建议合并>`（仅列非零，如「一般问题×1，轻微问题×1」）

## 问题清单

<按 致命问题→严重问题→一般问题→轻微问题→可选改进 顺序，仅输出有条目的组；空组整体跳过，不输出组标题>

### 致命问题

#### 问题A1：`<标题>`

- 描述：`<问题>`。当 `<触发条件>` 时，`<后果>`（见 `file.go:120`）
- 代码位置：
  - `file.go:120-135`（`funcName`；历史代码加 `（存量）`）
- 修复建议：`<可直接执行的动作>`

  ```diff
  - <改动前>
  + <改动后>
  ```

- 验证方法：`<复现步骤 / 回归测试 / 断言点；E 级填 不适用>`
- 状态：`<新问题 / 已修复 / 未修复 / 撤回>`

## 待确认

- `<事项>`：缺 `<信息>`；需确认 `<问题>`；若最坏情况成立则归 `<级别>`
